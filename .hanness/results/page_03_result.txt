=== page_03 — Game Loop & Spawn Manager ===
실행 시각: 2026-04-06T20:40:06
종료 코드: 0
============================================================
All files verified. Here's the summary:

---

## 생성/수정된 파일

### `lib/logic/spawn_manager.dart` (수정 — stub 대체)
- `SpawnConfig`: type, minInterval, maxInterval, cap, initialDelay 필드
- `SPAWN_TABLE`: 바위/나무/토끼/사슴/늑대/곰 6종 스폰 설정 (데이터 테이블)
- `SpawnManager.tick(dt, currentCounts)`: cap 초과 시 스폰 생략, 타이머 만료 시 랜덤 인터벌 재설정, 스폰할 `CardType` 목록 반환

### `lib/logic/win_lose_checker.dart` (신규)
- `WinLoseChecker.check(cards, gameTime)` 순수 함수
- 승리: `city>=1 && wall>=2 && (person+warrior)>=10`
- 패배: `gameTime>30 && population==0` (초기 30초 유예)
- `const winLoseChecker` 싱글턴 제공

### `lib/logic/game_loop.dart` (신규)
- `Stream.periodic(100ms)` 기반 Tick 루프
- 처리 순서 준수: gameTime → 타이머 감소 → 만료 처리(위협 expiryDamage) → SpawnManager → WinLoseChecker
- 위협 만료 시 wolf=-3 HP, bear=-5 HP 성벽 분산 적용, 성벽 HP=0이면 파괴
- `syncState(state)` / `pause()` / `resume()` / `dispose()` API 제공

### `lib/providers/game_provider.dart` (신규)
- `GameNotifier extends Notifier<GameState>` — GameLoop 소유 및 stream 구독
- `startNewGame()`: 초기 보드(사람2, 전사1, 식량/목재/돌) 생성 후 루프 시작
- `loadGame(saved)`, `pauseGame()`, `resumeGame()`, `applyExternalUpdate(state)` API
- `gameProvider = NotifierProvider<GameNotifier, GameState>` 글로벌 프로바이더

---

**`flutter analyze` 주의**: 이 환경에서 Flutter CLI가 PATH에 없어 실행할 수 없었습니다. 직접 실행이 필요하면 프로젝트 디렉토리(`cardforge/`)에서 `flutter analyze`를 수동으로 실행해 주세요.
